#include <stdio.h>

/**
 * main - this os the main entry point
 * feel free to add other things
 *
 * Return: returns 0 asthe exit code
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
